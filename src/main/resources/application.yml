server:
  port: 8081

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/carlosdangond1082/-config-repo
          clone-on-start: true
